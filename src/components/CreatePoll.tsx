import * as React from 'react';

type Props = {

};

export default function CreatePoll(props: Props) {
  return (
    <div>
      This is a create poll screen, can't you tell? :)
    </div>
  );
};

